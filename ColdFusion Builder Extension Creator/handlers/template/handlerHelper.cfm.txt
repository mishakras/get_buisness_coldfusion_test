<cfif isDefined("url.genId")>
	<cfset ideeventinfo = application[#url.genid#]>
	<cfset application[#url.genid#] = "">
	
	<cfset StructClear(Session)>
	
	<cfset requestProcessor = createObject("component","framework.RequestProcessor") >
	
	<cfset requestInfo = requestProcessor.parseRequest(#ideeventinfo#)>
	
	<cfset performAction(requestInfo)>
		
<cfelse>
	<h2>Cannot process the request. Handler is called without URL parameter 'genId'.</h2> 
</cfif>

<cffunction name="performAction">
	<cfargument name="requestInfo" required="true">
	
	<!--- Write your handler processing logic here --->
	<cfdump var="#arguments.requestInfo#">
</cffunction>
